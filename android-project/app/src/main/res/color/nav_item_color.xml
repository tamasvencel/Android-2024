<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Active state (selected item) -->
    <item android:color="@color/dark_orange" android:state_checked="true" />
    <!-- Inactive state (unselected item) -->
    <item android:color="@color/better_white" android:state_checked="false" />
</selector>